# future.ai

```md
# Future AI — Research Internship Agent


**Quick start**


1. Copy the files into a new directory or clone the repo.
2. Create a `.env` file (or set env vars) with your GEMINI key (see `.env.example`).


```bash
cp .env.example .env
# then edit .env and paste your key
npm install
npm run dev
```


Open http://localhost:5173


**Notes**
- The app expects a Vite env var named `VITE_GEMINI_API_KEY`.
- API keys should never be committed to public repos. Use GitHub secrets for CI and `.env` locally.
```
```


---
